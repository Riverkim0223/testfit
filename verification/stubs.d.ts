/**
 * Intentionally empty compatibility file.
 *
 * Studio v2 accidentally shipped ambient React, Next.js and Node.js stubs.
 * Those stubs replaced real package types with `any` in the application build.
 * The placeholder remains so applying this patch also neutralizes older copies.
 * Install dependencies and run `npm run typecheck` using the real package types.
 */
export {};
